node_modules/
dist/
dev-dist/
.env
*.local
*.tsbuildinfo
.DS_Store
data/stops.json
data/gate-history.json
.vercel/
# Vercel writes pulled environments here. Not .env* — that would swallow
# .env.example, which is meant to be tracked.
.env.local
.env*.local
.env*
